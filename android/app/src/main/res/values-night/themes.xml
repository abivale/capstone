<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Dark mode application theme. -->
    <style name="Theme.DFruitz.Dark" parent="Theme.MaterialComponents.DayNight">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/red_dark</item>
        <item name="colorPrimaryVariant">@color/deep_red</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/red_medium</item>
        <item name="colorSecondaryVariant">@color/teal_dark_red</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your dark mode theme here. -->
    </style>

    <style name="Wc.Dark" parent="Wc">
        <item name="android:textColor">@color/red_dark</item>
    </style>
</resources>
